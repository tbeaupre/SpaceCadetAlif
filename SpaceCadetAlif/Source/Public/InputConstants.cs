
namespace SpaceCadetAlif.Source.Public
{
    /*
     * An enum containing all inputs that the Actors may resond to.
     * These are not physical keys, but the functions associated with key presses.
     */

    enum Input
    {
        Left,
        Right,
        Up,
        Down,
        Jump,
        Attack
    }
}
